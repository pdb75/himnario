import 'package:flutter/material.dart';

class TransposeBar extends StatelessWidget {
  const TransposeBar({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
